#include "Address.hpp"

Address::Address(int houseNumber, string street, string city, string state, string country) {
    this.houseNumber = houseNumber;
    this.street = street, this.city = city, this.state = state, this.country = country;
}